name: OnSchedule

on:
  schedule:
  - cron:  '*/5 * * * *'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a multi-line script
      run: |
        hostname
        uname -a
        date
